@page "/ExternalLogin"
@model ExternalLoginModel
@{
    ViewData["Title"] = "Register";
}

<h1>@ViewData["Title"]</h1>
<h2 id="external-login-title">Associate your @Model.ProviderDisplayName account.</h2>
<hr />

<p id="external-login-description" class="text-info">
    Bạn đã xác thực thành công với <strong>@Model.ProviderDisplayName</strong>.
    Vui lòng nhập địa chỉ email cho trang web này bên dưới và nhấp vào nút Đăng ký để hoàn tất
    đăng nhập.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="email" placeholder="Vui lòng nhập email." readonly/>
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.TenDangNhap" class="form-control" autocomplete="username" placeholder="Vui lòng nhập username." />
                <label asp-for="Input.TenDangNhap" class="form-label"></label>
                <span asp-validation-for="Input.TenDangNhap" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.MaNguoiDung" class="form-control" autocomplete="userID" placeholder="Vui lòng nhập userID." required/>
                <label asp-for="Input.MaNguoiDung" class="form-label"></label>
                <span asp-validation-for="Input.MaNguoiDung" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Register</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
